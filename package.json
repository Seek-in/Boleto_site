{
  "name": "boleto-site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "node scripts/sync-guide-assets.mjs && astro dev",
    "build": "node scripts/sync-guide-assets.mjs && astro build",
    "build:site": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "node scripts/sync-guide-assets.mjs && astro build && rsync -avz --delete --exclude 'guide/' ./dist/ seek@168.119.165.218:/var/www/boleto/"
  },
  "dependencies": {
    "astro": "^7.0.5",
    "marked": "^18.0.6"
  }
}
